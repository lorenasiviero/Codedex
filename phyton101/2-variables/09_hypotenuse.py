a = int(input("Enter a: "))
b = int(input("Enter b: "))

c = (a**2 + b**2) ** 0.5

print(c)

# This program calculates the hypotenuse of a right triangle using the Pythagorean theorem.
# The user is prompted to enter the lengths of the two legs (a and b).
# The hypotenuse (c) is then calculated using the formula c = √(a² + b²).
# The result is printed to the console.